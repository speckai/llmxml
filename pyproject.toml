[project]
name = "llmxml"
version = "0.2.3"
description = "XML parsing for LLM outputs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.2",
]

[dependency-groups]
dev = [
    "black>=24.10.0",
    "pytest>=8.3.4",
]

[tool.setuptools]
packages = ["llmxml"]
