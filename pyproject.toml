[project]
name = "llmxml"
version = "0.1.5"
description = "XML parsing for LLM outputs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.2",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]

[tool.setuptools]
packages = ["llmxml"]
